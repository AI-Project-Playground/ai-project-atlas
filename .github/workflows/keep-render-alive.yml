name: Keep Render Alive

on:
  schedule:
    - cron: '5,20,35,50 * * * *'

  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send keep-alive request
        run: curl -f https://ai-project-atlas-api.onrender.com/
